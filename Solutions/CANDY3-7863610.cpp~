#include<iostream>
#include<stdio.h>
int main()
{
    int t,i;
    scanf("%d",&t);
    while(t--)
    {   printf("\n");
        long long int sum=0;
        long long int num;
       int n;
        scanf("%d",&n);
        for(i=0;i<n;i++)
        {
            scanf("%lld",&num);
            if(num>=0){
            sum=(sum+num)%n;}
        }
        if(sum==0)
        {printf("YES\n"); }
        else if(sum){printf("NO\n");}
    }
    return 0;
}

